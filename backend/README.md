# FROMO Backend

## Setup
1. Install uv
2. Run `uv sync`
3. Copy `.env.example` to `.env` and fill in values
4. Run `uvicorn main:app --reload`

## Stack
- FastAPI
- SQLAlchemy + Alembic (not sure)
- PostgreSQL (Supabase)
- uv