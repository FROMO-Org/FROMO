# Supabase project — Dashboard → Project Settings → API
# Service role key (not the publishable/anon key) — this script needs to
# bypass RLS to delete/insert into busyness_scores as a trusted batch job.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
