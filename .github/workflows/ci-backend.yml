name: Backend CI

on:
  push:
    branches: ["main"]
    paths: ["backend/**"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up runtime
        run: echo "TODO - add setup step once framework is decided"

      - name: Install dependencies
        run: echo "TODO - npm install OR pip install -r requirements.txt"

      - name: Run tests
        run: echo "TODO - npm test OR pytest"
