name: Mobile CI

on:
  push:
    branches: ["main"]
    paths: ["mobile/**"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up environment
        run: echo "TODO - Flutter setup OR React Native setup"

      - name: Install dependencies
        run: echo "TODO - flutter pub get OR npm install"

      - name: Run tests
        run: echo "TODO - flutter test OR npx react-native test"

      - name: Build app
        run: echo "TODO - flutter build OR react-native build"
